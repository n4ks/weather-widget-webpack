import { debounce } from '@/utils/base/debounce';
import { isObjectEmpty } from '@/utils/base/isObjectEmpty';
import { truncateText } from '@/utils/base/truncateText';
import { ls } from '@/utils/base/localStorage';

export const utils = { debounce, isObjectEmpty, truncateText, ls };
